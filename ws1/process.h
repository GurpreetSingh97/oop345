/*!
PROCESS_H, a header guard to prevent repeated use, help in maintaning one-definition rule in Process file
*/
#ifndef PROCESS_H
#define PROCESS_H

/*! 
a function that makes cString type objects from c-style null terminated char array
*/

void process(char str[]);

#endif // !W1_PROCESS_H
